import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'interview-dm-ng-modules',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
